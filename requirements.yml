---
# Installed by Controller on project sync. Keep this list to collections
# that job templates need inside the execution environment.
# ansible.controller is only required on the machine that runs
# playbooks/install.yml, and must already be present there.
collections:
  - name: ansible.posix
    version: ">=1.5.0"
